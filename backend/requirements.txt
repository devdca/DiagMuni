# actualizado por CVEs conocidos en la versión anterior de starlette
fastapi==0.141.1
starlette==1.6.0
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
alembic==1.14.0
# psycopg es LGPL-3.0, no GPL — LGPL permite explícitamente el linking dinámico sin
# obligar a que el proyecto que lo usa también sea GPL (a diferencia de GPL a secas).
# Es más permisivo que el carve-out "GPL solo como servicio separado" del Principio 2
# de docs/stack-tecnologico.md, no una excepción a él.
psycopg[binary]==3.2.10
pydantic==2.10.4
pydantic-settings==2.7.1
# 2.13.0 -- corrige CVE-2026-32597 (header `crit` no validado) y CVE-2026-48526
# (confusión de algoritmo HMAC/JWK), además de los avisos previos que la misma
# release agrupa (CVE-2026-48522/48523/48524/48525). Verificado que este proyecto
# solo usa HS256 simétrico (app/core/security.py) -- no se ve afectado por el
# vector directo de la confusión de algoritmo, pero se actualiza igual por ser
# la única dependencia que firma/verifica la autenticación de la plataforma.
pyjwt==2.13.0
argon2-cffi==23.1.0
pyyaml==6.0.2
# litellm: MIT (docs/stack-tecnologico.md, "Nota sobre LiteLLM (licencia)"), verificado
# contra github.com/BerriAI/litellm/LICENSE -- DiagMuni solo usa el SDK core
# (litellm.completion), nunca el paquete separado `enterprise/` (licencia propietaria).
# Versión 1.94.0 confirmada como la más reciente en PyPI el 30-jul-2026 (`pip index
# versions litellm`, y github.com/BerriAI/litellm/releases). Nota de seguridad
# verificada de forma independiente contra el aviso oficial del proyecto
# (docs.litellm.ai/blog/security-update-march-2026), GitHub Security Advisory
# GHSA-5mg7-485q-xm76 y análisis de terceros (Snyk, NetSPI): el 24-mar-2026 dos
# versiones puntuales (1.82.7 y 1.82.8) fueron comprometidas en PyPI mediante
# credenciales de CI robadas y puestas en cuarentena ~40 minutos después de su
# publicación; no afecta a 1.94.0 (muy posterior) ni a ninguna versión fijada aquí.
litellm==1.94.0
# fpdf2: LGPL-3.0 (github.com/py-pdf/fpdf2/blob/master/LICENSE) -- mismo criterio
# que psycopg[binary] arriba: uso dinámico vía pip install, no modificación ni
# linking estático de la librería. Python puro, sin librerías nativas del SO
# (a diferencia de WeasyPrint/Pango-Cairo) -- Docker de backend no cambia.
fpdf2==2.8.7
# cryptography: licencia dual Apache-2.0 OR BSD-3-Clause (verificado vía
# `pip show cryptography`, campo License-Expression) -- ambas permisivas, ninguna
# alcanzada por el gate de licencias GPL/AGPL de CI. Cifra las credenciales de IA
# que cada gobierno trae consigo (BYOK, ver app/core/cifrado.py) antes de
# guardarlas en `tenant` -- nunca se guardan en texto plano.
cryptography==50.0.1
# httpx: BSD-3-Clause -- ya llegaba de forma transitiva vía litellm/fastapi
# TestClient, se declara directo porque app/adaptadores/inegi/cliente_inegi.py
# ahora lo importa explícitamente (llamada real a la API de Indicadores de
# INEGI). Versión confirmada como la ya instalada en este entorno (`pip show
# httpx`) para no introducir un cambio de versión de paso.
httpx==0.28.1
